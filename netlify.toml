[build]
  command = "npm run build"
  publish = "dist"

# Serve real HTML files directly (pipeline.html, index.html)
[[redirects]]
  from = "/*.html"
  to = "/:splat.html"
  status = 200

# SPA fallback for client-side routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

